# Development

Run the checks with `python -m pytest` and `python -m compileall .`. The `reset_database` function accepts a scenario and creates a complete synthetic enterprise fixture. Keep business authorization in `services/enterprise.py`; agent code can select only actions that the service independently permits.
